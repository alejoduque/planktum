1.cambio en local el docu
2.git add .
3.git commit -m "nombre del commit"
4.git status (me dira que no hay nada que hacer porque el commit sucede en local)
5.git push origin master 

nota: una vez creado en nuevo master, se debe recordar hacer un remote add:
git remote add origin git@github.com:alejoduque/nmt.git


con esos 5 pasos tendre todo en la web de github.

si hago algun cambio en github via webinterface, debo hacer un:

git pull origin master 

para poner todo en sincro.

notas previas:

entonces debo hacer:

git pull origin master

para hacer un update de las modificaciones hechas via la interface web de github, asi cuando intento hacer:

git push origin master

sin que me salga el error:

 ! [rejected]        master -> master (non-fast forward)

ESTE TEXTO ES PARA PROBARLO

!!comprobado ahora lo intento en local y luego hago el push.

al intentar el git push me dice que debo hacer git add . 
lo hago e intento el push pero no sube nada
git status me dice: nothing to commit, directory clean


---

para clonar:
git pull github master

para iniciar (subir):
git push github master

despues de alguna modificacion:
git add .
git commit -m "w500_notes"

git rm xxx borra en local y remoto
(despues de hace el git add . git commit git push)

---

added rsa from user, this is a test
it works from kdag on the w500

---

para salir de este error:
error: Error building trees; the index is unmerged?

hago un simple
dspstv@localhost ~/tree/talleres_reciclables $ git commit -a

escojo lo que quiero que cambie en ese file que se abre y hago el push sin problemas

---


